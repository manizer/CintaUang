@model IEnumerable<Model.Domain.AlertMessage>
@foreach (var item in Model)
{
    <text>
        <script>
            $(document).ready(function () {
                showAlert("@item.Type", "@item.Message", "@item.Form");
            })
        </script>
    </text>
}